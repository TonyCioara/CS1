from person import *
from virus import *

"""
def create_person():
    virus1 = Virus("Ebola", 0.3, 0.5)
    return Person(5, True, virus1)


def test_person_init():
    test_subject = create_person()
    assert test_subject.is_vaccinated is True
    assert test_subject._id == 5
    assert test_subject.infected.name == "Ebola"


def test_did_survive_infection():
    test_subject = create_person()
    did_survive = test_subject.did_survive_infection()
    if did_survive is True:
        assert test_subject.is_vaccinated is True
    else:
        assert test_subject.is_alive is False
"""
